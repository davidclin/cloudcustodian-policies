policies:
  - name: iam-users-in-admin-group
    resource: iam-user
    description: |
      Retrieve list of IAM users in the group named 'Administrators'.
    filters:
      - type: group
        key: GroupName
        value: Administrators
